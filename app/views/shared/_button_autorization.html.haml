- if !current_user.present? && !current_page?(sessions_new_path)
    = link_to "", sessions_new_path, class: "btn icon-auth margin_top_15_px"
    -# =link_to t('auth'), sessions_new_path, class: "btn btn-ligth"
