- if current_user.present?  && !current_user.rule.nil? && current_user.rule.manager
  = form_for "", remote: true, url: { controller: "users/steps", action: "list"} do |f|
    = f.submit "", class: "icon-list-users btn margin_top_15_px"
  -# = link_to t('list_users'), users_list_path, class: "icon-list-users"
